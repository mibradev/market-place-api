Order #<%= @order.id %>

You ordered <%= pluralize(@order.products.count, "product") %>:

<% @order.products.each do |product| %>
  • <%= product.title %> - <%= number_to_currency product.price %>
<% end %>
